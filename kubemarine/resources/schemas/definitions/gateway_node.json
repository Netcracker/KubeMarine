{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "allOf": [{"$ref": "node_defaults.json#/definitions/SSHAccessCommonProperties"}],
  "properties": {
    "name": {
      "type": "string",
      "description": "The gateway node name."
    },
    "address": {
      "type": "string",
      "description": "The gateway node's IP or hostname address for connection."
    }
  },
  "required": ["name", "address", "username"],
  "oneOf": [
    {
      "required": ["keyfile"]
    },
    {
      "required": ["password"]
    }
  ],
  "propertyNames": {
    "anyOf": [
      {"$ref": "node_defaults.json#/definitions/SSHAccessCommonPropertyNames"},
      {"enum": ["name", "address"]}
    ]
  }
}
